{
    "ignorePatterns": [
		{
			"pattern": "^http://localhost:*"
		}
    ],
    "replacementPatterns": [
        {
            "pattern": "^/",
            "replacement": "/github/workspace/"
        }
    ]
}